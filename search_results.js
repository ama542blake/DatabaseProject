/*
    This script is to allow submission of a form by clicking a link (well, really just a <p> styled to look like a link)
    on the search results page. Creating a button for every search result would be really ugly.
*/

window.onload = function() {
    // STEP 1: 
}